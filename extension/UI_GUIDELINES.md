# LINEOA 擴充功能頁面規範

## 離開功能頁

每一個獨立功能頁面都必須提供清楚且可見的離開方式：

- 至少一個「返回」導覽，回到上一層 LINEOA 頁面。
- 或提供「取消／關閉」按鈕；建議兩者皆有。
- 長頁面的離開控制必須固定在頁首，使用者捲動後仍能看見。
- 不可只依賴瀏覽器上一頁、分頁關閉鈕或鍵盤快捷鍵。

目前套用頁面：

- `options.html`
- `menu.html`
